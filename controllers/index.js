const matchController = require('./match.controller');
const constructedTeamController = require('./constructedTeam.controller');
const playerController = require('./player.controller');
module.exports = {
    matchController,
    constructedTeamController,
    playerController
}